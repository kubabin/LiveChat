// ---------- Season tab (disabled example) ----------
// This tab has no dynamic behavior yet - it's here to show the minimum
// a new tab needs: an entry in tabs.config.js, a tabs/<id>.html file,
// and (optionally) this init stub.

window.TAB_MODULES = window.TAB_MODULES || {};
window.TAB_MODULES.season = {
  init(panelEl) {
    // no-op
  },
};
