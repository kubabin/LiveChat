// ---------- Map tab ----------
// The map is just an iframe, so there's nothing to initialize.
// Kept as a stub so every tab follows the same init(panelEl) contract.

window.TAB_MODULES = window.TAB_MODULES || {};
window.TAB_MODULES.map = {
  init(panelEl) {
    // no-op
  },
};
